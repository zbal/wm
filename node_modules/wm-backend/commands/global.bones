var _ = require('underscore');
var path = require('path');

var defaultPath = function(name) {
    return path.resolve(require.resolve('wm-backend'), '../../../'+ name);
};

Bones.Command.options['files'] = {
    'title': 'files=[path]',
    'description': 'Path to files directory.',
    'default': defaultPath('files')
};

Bones.Command.options['stash'] = {
    'title': 'stash=[path]',
    'description': 'Path to stash directory.',
    'default': defaultPath('conf')
};