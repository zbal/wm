models.Checks.prototype.sync = function(method, model, options) {
    var success = options.success, error = options.error;

    var Stash = new Bones.plugin.backends.Stash('check');
    Stash.sync.apply(this, arguments);
};
