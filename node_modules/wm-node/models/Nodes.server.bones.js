models.Nodes.prototype.sync = function(method, model, options) {
    var success = options.success, error = options.error;

    var Stash = new Bones.plugin.backends.Stash('node');
    Stash.sync.apply(this, arguments);
};

models.Nodes.prototype.initialize = function(options, param) {
    console.log('[Debug Nodes] options: ', options);
    console.log('[Debug Nodes] param: ', param);
    return this;
}